<div class="avatar-form-page">
  <div class="avatar-form-page__header">
    <%= link_to "← Back to all avatars", avatars_path, class: "avatar-show__back-link" %>
  </div>

  <h1>Edit Avatar</h1>

  <p>Update your avatar's details and save the changes.</p>

  <%= render "avatar_image" %>

  <%= render "form", form_object: @avatar_form, form_url: avatar_path(@avatar_form), form_method: :patch, submit_label: "Save Avatar" %>

  <%= render "submission_summary", avatar: @current_avatar %>
</div>
