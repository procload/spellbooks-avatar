<%# Eldritch UI Banner Component
    Renders a banner message with golden background and borders
    Supports variants (success, warning, error), sizes, and alignment options
%>
<%= content_tag tag, **component_tag_attributes do %>
  <%= message || content %>
<% end %> 